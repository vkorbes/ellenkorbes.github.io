// Title: Slasher Flick
// Prompt: Return the remaining elements of an array after chopping off n elements from the head.
// URL: https://www.freecodecamp.com/challenges/slasher-flick

function slasher (arr, howMany) {
  var variable = arr.slice(howMany)
  return variable
}
